#include "Validator.h"
#include "Board.h"

bool Validator::isRowValid(const Board& board, int row, int value) { return true; }
bool Validator::isColValid(const Board& board, int col, int value) { return true; }
bool Validator::isBoxValid(const Board& board, int row, int col, int value) { return true; }
